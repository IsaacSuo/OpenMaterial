# 2DGS-PBR CUDA extensions
# MUST be installed AFTER PyTorch is installed
# Run: pip install --no-build-isolation -r requirements-extensions.txt
# Note: Use --no-build-isolation if torch is not found during build

./submodules/diff-surfel-rasterization
./submodules/simple-knn
